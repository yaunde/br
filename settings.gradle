rootProject.name = 'br'

include 'br-controller'
include 'br-core'
include 'br-web'
include 'br-services'

rootProject.children.each {project ->
    project.buildFileName = "${project.name}.gradle"
}


